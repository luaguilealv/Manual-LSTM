# Manual-LSTM
From-scratch LSTM network with custom-coded BPTT gradient updates.
Run try_LSTM_1.m it has the generated data and calls the functions needed for the training.
my_lstm.m is the function with the training using SGD
